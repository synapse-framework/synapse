{
  "name": "{{projectName}}",
  "version": "0.1.0",
  "description": "{{description}}",
  "type": "module",
  "scripts": {
    "dev": "synapse dev",
    "build": "synapse build",
    "test": "synapse test",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@snps/core": "^0.1.0"
  }
}
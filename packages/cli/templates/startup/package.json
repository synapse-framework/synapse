{
  "name": "{{projectName}}",
  "version": "0.1.0",
  "description": "{{description}}",
  "type": "module",
  "scripts": {
    "dev": "synapse dev",
    "build": "synapse build",
    "deploy": "synapse deploy",
    "test": "synapse test"
  },
  "dependencies": {
    "@snps/core": "^0.1.0"
  }
}
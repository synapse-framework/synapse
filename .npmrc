# NPM Configuration for Synapse Framework
# This file configures NPM for publishing to the registry

# Use the official NPM registry
registry=https://registry.npmjs.org/

# Enable package-lock.json
package-lock=true

# Save exact versions
save-exact=false

# Use auth token from environment
# Set NPM_TOKEN environment variable before publishing
//registry.npmjs.org/:_authToken=${NPM_TOKEN}
